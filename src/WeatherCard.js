import React from 'react';

const WeatherCard = (props) => {
  return(
    <section className="MenuitemCardSection">
      <p>hier komt het weer</p>
    </section>
  );
}

export default WeatherCard;
